# Personal Config for Neovim

This config is meant to be used with [Nix](https://nixos.org/).
All plugins are installed using [Nix](https://nixos.org/) instead of other plugin managers.
However, I do still like doing the configuration separately with lua.
